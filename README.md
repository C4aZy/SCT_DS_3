# SCT_DS_3
Built a Decision Tree classifier on the UCI Bank Marketing dataset to predict customer subscription behavior. Performed data preprocessing, handled class imbalance, removed data leakage, applied one-hot encoding, evaluated with confusion matrix and classification report, and analyzed feature importance for business insights.
